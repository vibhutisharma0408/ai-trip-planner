[build]
  base = "."
  command = "npm run build"
  # Netlify Next.js plugin handles the publish directory automatically

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

